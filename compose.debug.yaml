services:
  wire-pod:
    volumes:
      - ${WIREPOD_DEBUG_SRC:-./wire-pod-debug}:/opt/wire-pod-debug
      - wire-pod-debug-tools:/opt/dev-tools
    command: ["tail", "-f", "/dev/null"]
    healthcheck:
      disable: true

volumes:
  wire-pod-debug-tools:
    driver: local
